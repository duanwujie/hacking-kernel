\chapter{预测算法}


\begin{itemize}
\item 问题描述：根据历史数据，预测未来所产生数据可能的值。由于历史数据具有周期性。因此这里通过两种算法对数据进行预测，一通过历史区间来预测数据，二通过数据周期来预测数据。
\end{itemize}


\section{数学说明}

定义$X_0 , X_1 , X_2 , X_3 , .....X_{n-1}$一系列离散的点，通过1次拟合、2次拟合或n次拟合来计算$p_n$最可能的值。

定义期望:
$$\mu=\frac{X_0+X_1+X_2+ ... X_{n-1}}{n-1}$$

其中求和:
	$$sum = X_0+X_1+X_2+ ... X_{n-1}$$
其中点的个数:
	$$count = n-1;$$
则：$\mu=\frac{sum}{count}$

对期望做增量求值:

	$$\mu_n = \frac{sum_{n-1}+ X_n}{count_{n-1}+1}$$
	
方差：
$$ \sigma^{2}_{n-1} = \frac{\sum\limits_{i=1}^{N-1}(X_{i}-\mu)^{2}}{N-1}$$


增量方差:
	$$\mu_{n+1} = \frac{count_{n-1}[\mu_{n-1}+(]

$$
\sigma^2 = \frac { M\left[\sigma_H^2 + \left(\overline{X} - \overline{H}\right)^2\right] + N\left[\sigma_A^2 + \left(\overline{X} - \overline{A}\right)^2\right] } {M + N} \nonumber
$$

